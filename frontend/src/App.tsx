import { Loading } from './components/Common/Loading/Loading.tsx';

export const App = () => {
  return (
    <div>
      <h1 className="text-black">Hello, world!</h1>
      <Loading />
    </div>
  );
};
